export enum M1Options {
    MAR,
    PC,
    SP,
    M4,
}

export enum M2Options {
    ula,
    SP,
    M4,
    memory,
    KEYBOARD,
}

export enum M3Options {
    FR,
    R0,
    R1,
    R2,
    R3,
    R4,
    R5,
    R6,
    R7,
}

export enum M4Options {
    R0,
    R1,
    R2,
    R3,
    R4,
    R5,
    R6,
    R7,
}

export enum M5Options {
    PC,
    M3,
}

export enum M6Options {
    memory,
    ula_flag,
}
